local _fighting_module = CS.Aquila.GameEntry.Module:GetModule(typeof(CS.Aquila.Module.Module_Scene))
local _terrain_module = _fighting_module.Terrain_Module
local Debug = CS.UnityEngine.Debug


--- 启动函数
function on_start_lua()

    Debug.Log("Into lua")
    local terrain = _terrain_module:Get(0,0)
    if(terrain ~= nil) then
        Debug.Log("terrain is not null")
        terrain:SetName("call_from_lua")
    end
    
end

--- 计时回调
function on_timer_tick_lua(delta_time)
    
end

--- 结束回调
function on_finish_lua()
    
end

--刷帧
function on_update_lua()

end

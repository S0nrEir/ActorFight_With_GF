local _terrain_module = CS.Aquila.GameEntry.Module:GetModule(typeof(CS.Aquila.Module.Module_Terrain))
local Debug = CS.UnityEngine.Debug

function on_start_lua()
    Debug.Log("Into lua")

    local terrain = _terrain_module:Get(1,1)
    if(terrain ~= nil) then
        Debug.Log("terrain is not null")
    end
    
end

function on_timer_tick_lua(delta_time)
    
end

function on_finish_lua()
    
end

function on_update_lua()

end

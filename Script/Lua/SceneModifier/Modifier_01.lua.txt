local _scene_module = CS.Aquila.GameEntry.Module:GetModule(typeof(CS.Aquila.Module.Module_Scene))
local Debug = CS.UnityEngine.Debug

function on_start_lua()
    Debug.Log("Into lua")

    -- if(_terrain_module == nil) then
    --     Debug.Log("_terrain_module is null")
    -- else
    --     Debug.Log("_terrain_module is not null")
    -- end

    local terrain = _scene_module.Terrain_Module:Get(1,1)
    if(terrain ~= nil) then
        Debug.Log("terrain is not null")
        terrain:SetName("call_from_lua")
    end
    
end

function on_timer_tick_lua(delta_time)
    
end

function on_finish_lua()
    
end

function on_update_lua()

end
